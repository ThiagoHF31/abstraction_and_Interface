/**
 * A interface estudante conterá o conjunto de métodos que
 * devem ser implementados por qualquer entidade no projeto
 * que realize estas ações. Note que por padrão seus métodos
 * além de não terem implementação (são abstratos) eles também
 * são públicos.
 *
 */
public interface Estudante {
	void estudar();
}
