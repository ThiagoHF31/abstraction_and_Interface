/**
 * A interface FuncionárioAsslariado conterá o conjunto de métodos que
 * devem ser implementados por qualquer entidade no projeto
 * que realize recebe salário. Note que por padrão seus métodos
 * além de não terem implementação (são abstratos) eles também
 * são públicos.
 *
 */
public interface FuncionárioAssalariado {
	void receberSalário(int nTurmas);
}
